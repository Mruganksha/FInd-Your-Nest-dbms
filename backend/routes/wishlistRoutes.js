import express from 'express';import { addWishlist, getWishlist, removeWishlist } from '../controllers/wishlistController.js';import { verifyToken } from '../middleware/authMiddleware.js';const router=express.Router();router.post('/',verifyToken,addWishlist);router.get('/',verifyToken,getWishlist);router.delete('/:id',verifyToken,removeWishlist);export default router;
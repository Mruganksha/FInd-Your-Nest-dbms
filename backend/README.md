# Find Your Nest - Backend (Express + MySQL)

## Quick Start
1. Run `npm install`
2. Update `.env` with your MySQL credentials
3. Run `npm run dev`
4. API base URL: http://localhost:5000/api

Includes:
- Authentication (JWT)
- Listings CRUD
- Reviews, Wishlist, Booking, Reports
